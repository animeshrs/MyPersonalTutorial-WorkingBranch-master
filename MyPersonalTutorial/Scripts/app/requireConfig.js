require.config({
    baseUrl: '/Scripts',
    waitSeconds: 0,
    paths: {
        /* Mediators */
        mediator: 'app/mediator/mediator',
        channels: 'app/mediator/channels',

        /* Libraries */
        jquery: 'jquery-1.10.2',
        knockout: 'knockout-3.4.2',
        amplify:'amplify'
    }
});